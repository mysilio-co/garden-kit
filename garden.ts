import { GardenIndex } from "./types";

function createNewSubindex(mainIndexIri: GardenIndex, subIndexIri: GardenIndex) {
  // setup an empty Garden subindex at the given subindex iri
  // linked to the given main index iri
}

function createNewIndex(iri: GardenIndex) {
  // setup an empty Garden index at the given iri
}